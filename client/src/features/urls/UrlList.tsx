import type { Url } from '../../lib/api'; export function UrlList({urls,onDelete}:{urls:Url[];onDelete:(id:string)=>Promise<void>}){return <section><h2>Your shortened URLs</h2>{urls.length===0?<p>No URLs yet.</p>:<ul>{urls.map(url=><li key={url.id}><a href={url.shortUrl} target="_blank">{url.shortUrl}</a><span>{url.originalUrl}</span><button onClick={()=>void onDelete(url.id)}>Delete</button></li>)}</ul>}</section>}
