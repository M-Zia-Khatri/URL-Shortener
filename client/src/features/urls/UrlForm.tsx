import { useState } from 'react'; export function UrlForm({onCreate}:{onCreate:(url:string,expiresAt?:string)=>Promise<void>}){const [url,setUrl]=useState(''),[expiresAt,setExpiresAt]=useState(''),[busy,setBusy]=useState(false);return <form onSubmit={async e=>{e.preventDefault();setBusy(true);try{await onCreate(url,expiresAt);setUrl('');setExpiresAt('')}finally{setBusy(false)}}}><input aria-label="URL to shorten" required type="url" placeholder="https://example.com" value={url} onChange={e=>setUrl(e.target.value)}/><input aria-label="Expiration" type="datetime-local" value={expiresAt} onChange={e=>setExpiresAt(e.target.value)}/><button disabled={busy}>{busy?'Shortening…':'Shorten URL'}</button></form>}
