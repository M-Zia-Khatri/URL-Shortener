declare module '@fastify/cors' { import { FastifyPluginCallback } from 'fastify'; const plugin: FastifyPluginCallback<any>; export default plugin }
declare module '@fastify/helmet' { import { FastifyPluginCallback } from 'fastify'; const plugin: FastifyPluginCallback<any>; export default plugin }
declare module '@fastify/swagger' { import { FastifyPluginCallback } from 'fastify'; const plugin: FastifyPluginCallback<any>; export default plugin }
declare module '@fastify/swagger-ui' { import { FastifyPluginCallback } from 'fastify'; const plugin: FastifyPluginCallback<any>; export default plugin }
