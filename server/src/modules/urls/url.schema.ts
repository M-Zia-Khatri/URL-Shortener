import { z } from 'zod'; export const idParams=z.object({id:z.string().uuid()}); export const codeParams=z.object({code:z.string().min(6).max(16)}); export const createUrlBody=z.object({originalUrl:z.string().min(1),expiresAt:z.coerce.date().optional()}); export const updateUrlBody=z.object({originalUrl:z.string().min(1).optional(),expiresAt:z.union([z.coerce.date(),z.null()]).optional()}).refine(v=>Object.keys(v).length>0); export const listQuery=z.object({page:z.coerce.number().int().positive().default(1),limit:z.coerce.number().int().min(1).max(100).default(20)}); export const urlResponse=z.object({id:z.string().uuid(),originalUrl:z.string(),shortCode:z.string(),shortUrl:z.string(),isActive:z.boolean(),expiresAt:z.date().nullable(),createdAt:z.date()});
